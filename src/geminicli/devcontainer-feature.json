{
  "id": "gemini-cli",
  "version": "0.1.0",
  "name": "Google Gemini CLI",
  "description": "Installs Google Gemini CLI for AI code assistance https://geminicli.com",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "proposals":[
        "latest",
        "nightly",
        "preview"
      ],
      "description": "Version of Gemini CLI to install https://www.npmjs.com/package/@google/gemini-cli#release-cadence-and-tags"
    },
    "node_major_version": {
      "type": "string",
      "default": "24",
      "description": "Major version of Node.js to use from NodeSource. Only used if the minimum required version of Node.js is not already installed. Must be 20 or higher."
    }
  },
  "containerEnv": {
    "PATH": "/usr/bin:$PATH"
  }
}

