{
  "id": "gemini-cli",
  "version": "0.1.0",
  "name": "Google Gemini CLI",
  "description": "Installs Google Gemini CLI for AI code assistance https://geminicli.com",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "proposals":[
        "latest",
        "nightly",
        "preview"
      ],
      "description": "Version of Gemini CLI to install https://www.npmjs.com/package/@google/gemini-cli#release-cadence-and-tags"
    },
    "node_major_version": {
      "type": "string",
      "default": "24",
      "description": "Major version of Node.js to use from NodeSource. Must be 20 or higher. Note this is only used if Node.js is not already installed."
    }
  }
}

