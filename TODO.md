test/_global/scenarios.json combines all features into a single devcontainer. Separate these out into many devcontainer scenarios, with one feature per scenario, and all 3 base images per feature in the collection
